These PDFs had parse errors in the extraction phase.

They are mostly GhostScript errors and potentially a Windows only thing.

Because they failed at the extraction phase they are unusable for anything else.
